<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    xmlns:behaviors="clr-namespace:Tools.View.Behaviors">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/Prehub.View;component/Themes/Generic.xaml" />
        <ResourceDictionary Source="pack://application:,,,/Prehub.View;component/Themes/SharedStyles.xaml" />
    </ResourceDictionary.MergedDictionaries>
    <Thickness x:Key="mNone">0</Thickness>
    <Thickness x:Key="mLeft">10 0 0 0</Thickness>
    <Thickness x:Key="mRight">0 0 10 0</Thickness>
    <Thickness x:Key="mTop">0 10 0 0</Thickness>
    <Thickness x:Key="mBottom">0 0 0 10</Thickness>
    <Thickness x:Key="mLeftRight">10 0 10 0</Thickness>
    <Thickness x:Key="mLeftBottom">10 0 0 10</Thickness>
    <Thickness x:Key="mTopBottom">0 10 0 10</Thickness>
    <Thickness x:Key="mTopLeftRight">10 10 10 0</Thickness>
    <Thickness x:Key="mTopLeft">10 10 0 0</Thickness>


    <Thickness x:Key="BorderControl">10 10 0 0</Thickness>


    <Thickness x:Key="mAll">10</Thickness>
    <sys:Double x:Key="w60">60</sys:Double>
    <sys:Double x:Key="w70">70</sys:Double>
    <sys:Double x:Key="w80">80</sys:Double>
    <sys:Double x:Key="w90">90</sys:Double>
    <sys:Double x:Key="w100">100</sys:Double>
    <sys:Double x:Key="w120">120</sys:Double>
    <sys:Double x:Key="w220">220</sys:Double>
    <sys:Double x:Key="h20">20</sys:Double>
    <sys:Double x:Key="h25">25</sys:Double>
    <sys:Double x:Key="h30">30</sys:Double>
    <Thickness x:Key="mLeftNe">-10 0 0 0</Thickness>
    <Thickness x:Key="mRightNe">0 0 -10 0</Thickness>
    <Thickness x:Key="mTopNe">0 -10 0 0</Thickness>
    <Thickness x:Key="mBottomNe">0 0 0 -10</Thickness>
    <Thickness x:Key="mLeftRightNe">-10 0 -10 0</Thickness>
    <Thickness x:Key="mLeftBottomNe">-10 0 0 -10</Thickness>
    <Thickness x:Key="mTopBottomNe">0 -10 0 -10</Thickness>
    <Thickness x:Key="mTopLeftRightNe">-10 -10 -10 0</Thickness>
    <Thickness x:Key="mTopLeftNe">-10 -10 0 0</Thickness>
    <Thickness x:Key="mAllNe">-10</Thickness>

    <FontFamily x:Key="Fonts.Family">Segoe UI, Lucida Sans Unicode, Verdana</FontFamily>

    <sys:Double x:Key="HeightControl">28</sys:Double>
    <sys:Double x:Key="Font.Size.Header">40</sys:Double>
    <sys:Double x:Key="Font.Size.SubHeader">29.333</sys:Double>
    <sys:Double x:Key="Font.Size.Window.Title">16</sys:Double>
    <sys:Double x:Key="Font.Size.Default">14</sys:Double>
    <sys:Double x:Key="Font.Size.Content">12</sys:Double>
    <sys:Double x:Key="Font.Size.Button.Flat">14</sys:Double>
    <sys:Double x:Key="Font.Size.TabItem">26.67</sys:Double>
    <sys:Double x:Key="Font.Size.Button">14</sys:Double>
    <sys:Double x:Key="Font.Size.FloatingWatermark">10</sys:Double>
    <sys:Double x:Key="Font.Size.Button.ClearText">16</sys:Double>
    <sys:Double x:Key="Font.Size.Tooltip">12</sys:Double>
    <sys:Double x:Key="Font.Size.Menu">14</sys:Double>
    <sys:Double x:Key="Font.Size.ContextMenu">14</sys:Double>
    <sys:Double x:Key="Font.Size.StatusBar">12</sys:Double>
    <sys:Double x:Key="Font.Size.Dialog.Title">26</sys:Double>
    <sys:Double x:Key="Font.Size.Dialog.Message">15</sys:Double>
    <sys:Double x:Key="Font.Size.Dialog.Button">12</sys:Double>
    <sys:Double x:Key="Font.Size.Flyout.Header">20</sys:Double>
    <sys:Double x:Key="Font.Size.ToggleSwitch">14</sys:Double>
    <sys:Double x:Key="Font.Size.ToggleSwitch.Header">14</sys:Double>
    <sys:Double x:Key="Font.Size.ColorPicker.TabItemHeader">16</sys:Double>
    <Thickness x:Key="StrockThicknessActicve">1</Thickness>


    <SolidColorBrush x:Key="Brushes.ThemeForeground">#FF000000</SolidColorBrush>
    <SolidColorBrush x:Key="Brushes.ThemeBackground">#FFFFFFFF</SolidColorBrush>
    <SolidColorBrush x:Key="Brushes.IdealForeground">White</SolidColorBrush>
    <!--  ACCENT COLORS  -->
    <Color x:Key="Colors.Highlight">#99b2ff</Color>
    <Color x:Key="Colors.AccentBase">#3366FF</Color>
    <!--  80%  -->
    <Color x:Key="Colors.Accent">#CC0078D7</Color>
    <!--  60%  -->
    <Color x:Key="Colors.Accent2">#990078D7</Color>
    <!--  40%  -->
    <Color x:Key="Colors.Accent3">#660078D7</Color>
    <!--  20%  -->
    <Color x:Key="Colors.Accent4">#330078D7</Color>
    <Color x:Key="Colors.Disabled">#A5FFFFFF</Color>
    <Color x:Key="Colors.MenuShadow">#FF000000</Color>
    <Color x:Key="Colors.ContextMenu.Border">#E0717070</Color>
    <Color x:Key="Colors.Border">#FFB1B1B1</Color>

    <SolidColorBrush x:Key="Brushes.Highlight" Color="{StaticResource Colors.Highlight}"  />
    <SolidColorBrush x:Key="Brushes.AccentBase" Color="{StaticResource Colors.AccentBase}"  />
    <SolidColorBrush x:Key="Brushes.Accent" Color="{StaticResource Colors.Accent}" />
    <SolidColorBrush x:Key="Brushes.Accent2" Color="{StaticResource Colors.Accent2}"  />
    <SolidColorBrush x:Key="Brushes.Accent3" Color="{StaticResource Colors.Accent3}" />
    <SolidColorBrush x:Key="Brushes.Accent4" Color="{StaticResource Colors.Accent4}" />
    <SolidColorBrush x:Key="Brushes.Disabled" Color="{StaticResource Colors.Disabled}" />
    <SolidColorBrush x:Key="Brushes.ContextMenu.Border" Color="{StaticResource Colors.ContextMenu.Border}"></SolidColorBrush>
    <SolidColorBrush x:Key="Brushes.Border" Color="{StaticResource Colors.Border}"></SolidColorBrush>



    <Color x:Key="Colors.Gray1">#FF333333</Color>
    <Color x:Key="Colors.Gray2">#FF7F7F7F</Color>
    <Color x:Key="Colors.Gray3">#FF9D9D9D</Color>
    <Color x:Key="Colors.Gray4">#FFA59F93</Color>
    <Color x:Key="Colors.Gray5">#FFB9B9B9</Color>
    <Color x:Key="Colors.Gray6">#FFCCCCCC</Color>
    <Color x:Key="Colors.Gray7">#FFD8D8D9</Color>
    <Color x:Key="Colors.Gray8">#FFE0E0E0</Color>
    <Color x:Key="Colors.Gray9">#5EC9C9C9</Color>
    <Color x:Key="Colors.Gray10">#FFF7F7F7</Color>
    <SolidColorBrush x:Key="Brushes.Gray1" Color="{StaticResource Colors.Gray1}"  />
    <SolidColorBrush x:Key="Brushes.Gray2" Color="{StaticResource Colors.Gray2}" />
    <SolidColorBrush x:Key="Brushes.Gray3" Color="{StaticResource Colors.Gray3}" />
    <SolidColorBrush x:Key="Brushes.Gray4" Color="{StaticResource Colors.Gray4}"  />
    <SolidColorBrush x:Key="Brushes.Gray5" Color="{StaticResource Colors.Gray5}"  />
    <SolidColorBrush x:Key="Brushes.Gray6" Color="{StaticResource Colors.Gray6}"  />
    <SolidColorBrush x:Key="Brushes.Gray7" Color="{StaticResource Colors.Gray7}" />
    <SolidColorBrush x:Key="Brushes.Gray8" Color="{StaticResource Colors.Gray8}"  />
    <SolidColorBrush x:Key="Brushes.Gray9" Color="{StaticResource Colors.Gray9}"  />
    <SolidColorBrush x:Key="Brushes.Gray10" Color="{StaticResource Colors.Gray10}"  />

    
    <Style  TargetType="Button" x:Key="ButtonBase">
        <Setter Property="Height" Value="{StaticResource HeightControl}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Brushes.Border}" />
        <Setter Property="BorderThickness" Value="1.5" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Padding" Value="10" />
        <Setter Property="FontFamily" Value="{DynamicResource Fonts.Family}" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
	                 BorderBrush="{TemplateBinding BorderBrush}"
	                 BorderThickness="{TemplateBinding BorderThickness}"
	                 CornerRadius="5">
                        <ContentPresenter x:Name="contentPresenter"
				   Content="{TemplateBinding Content}"
				   HorizontalAlignment="Center"
				   VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="contentPresenter" Property="Opacity" Value="0.8" />
                            <Setter Property="Background" Value="#fefefe" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="contentPresenter" Property="Opacity" Value="0.6" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="Gray" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style  TargetType="Button" BasedOn="{StaticResource ButtonBase}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
			                 BorderBrush="{TemplateBinding BorderBrush}"
			                 BorderThickness="{TemplateBinding BorderThickness}"
			                 CornerRadius="5">
                        <ContentPresenter x:Name="contentPresenter"
						   Content="{TemplateBinding Content}"
						   HorizontalAlignment="Center"
						   VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="contentPresenter" Property="Opacity" Value="0.8" />
                            <Setter Property="Background" Value="#fefefe" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="contentPresenter" Property="Opacity" Value="0.6" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="Gray" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="Button.OK" TargetType="Button" BasedOn="{StaticResource ButtonBase}">
        <Setter Property="Background" Value="{DynamicResource Brushes.AccentBase}" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
						 BorderBrush="{TemplateBinding BorderBrush}"
						 BorderThickness="{TemplateBinding BorderThickness}"
						 CornerRadius="5">
                        <ContentPresenter x:Name="contentPresenter"
									   Content="{TemplateBinding Content}"
									   HorizontalAlignment="Center"
									   VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="contentPresenter" Property="Opacity" Value="0.8" />
                            <Setter Property="Background" Value="{DynamicResource Brushes.Accent}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="contentPresenter" Property="Opacity" Value="0.6" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="Gray" />
                            <Setter Property="Background" Value="LightGray" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource Fonts.Family}"/>
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Left" />
    </Style>
    <Style TargetType="Label">
        <Setter Property="FontFamily" Value="{StaticResource Fonts.Family}"/>
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Left" />
    </Style>

    <Style  TargetType="RadioButton">
        <Setter Property="FontFamily" Value="{StaticResource Fonts.Family}"/>
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="Padding" Value="10" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="0" Width="20" Height="20" Background="Transparent">
                            <Ellipse x:Name="RadioButtonEllipse" Fill="White" Stroke="{DynamicResource Brushes.Border}" StrokeThickness="1.5" />
                        </Grid>
                        <ContentPresenter Grid.Column="1" Margin="5,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Center" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="RadioButtonEllipse" Property="Stroke" Value="{DynamicResource Brushes.AccentBase}" />
                            <Setter TargetName="RadioButtonEllipse" Property="StrokeThickness" Value="5" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="RadioButtonEllipse" Property="Stroke" Value="{DynamicResource Brushes.AccentBase}" />
                            <Setter TargetName="RadioButtonEllipse" Property="StrokeThickness" Value="5" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="RadioButtonEllipse" Property="Stroke" Value="{DynamicResource Brushes.Border}" />
                            <Setter TargetName="RadioButtonEllipse" Property="Fill" Value="Transparent" />
                            <Setter Property="Foreground" Value="Gray" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <SolidColorBrush x:Key="ComboBoxNormalBorderBrush" Color="#e3e9ef" />
    <SolidColorBrush x:Key="ComboBoxNormalBackgroundBrush" Color="#fff" />
    <SolidColorBrush x:Key="ComboBoxDisabledForegroundBrush" Color="#888" />
    <SolidColorBrush x:Key="ComboBoxDisabledBackgroundBrush" Color="#eee" />
    <SolidColorBrush x:Key="ComboBoxDisabledBorderBrush" Color="#888" />

    <ControlTemplate TargetType="ToggleButton" x:Key="ComboBoxToggleButtonTemplate">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="20" />
            </Grid.ColumnDefinitions>
            <Border Grid.ColumnSpan="2" Name="Border"
              BorderBrush="{StaticResource ComboBoxNormalBorderBrush}" 
              CornerRadius="0" BorderThickness="0" 
              Background="{StaticResource ComboBoxNormalBackgroundBrush}" />
            <Border Grid.Column="1" Margin="1, 1, 1, 1" BorderBrush="{DynamicResource  Brushes.Gray6}" Name="ButtonBorder"
              CornerRadius="0, 0, 0, 0" BorderThickness="0, 0, 0, 0" 
              Background="{StaticResource ComboBoxNormalBackgroundBrush}" />

            <Path Name="Arrow" Grid.Column="1" 
            Data="M0,0 L0,2 L4,6 L8,2 L8,0 L4,4 z"
            HorizontalAlignment="Center" Fill="{DynamicResource  Brushes.Gray6}"
            VerticalAlignment="Center" />
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="UIElement.IsMouseOver" Value="True">
                <Setter Property="Panel.Background" TargetName="ButtonBorder" Value="WhiteSmoke"/>
            </Trigger>
            <Trigger Property="ToggleButton.IsChecked" Value="True">
                <Setter Property="Panel.Background" TargetName="ButtonBorder" Value="WhiteSmoke"/>
                <Setter Property="Shape.Fill" TargetName="Arrow" Value="#FF8D979E"/>
            </Trigger>
            <Trigger Property="UIElement.IsEnabled" Value="False">
                <Setter Property="Panel.Background" TargetName="Border" Value="{StaticResource ComboBoxDisabledBackgroundBrush}"/>
                <Setter Property="Panel.Background" TargetName="ButtonBorder" Value="{StaticResource ComboBoxDisabledBackgroundBrush}"/>
                <Setter Property="Border.BorderBrush" TargetName="ButtonBorder" Value="{StaticResource ComboBoxDisabledBorderBrush}"/>
                <Setter Property="TextElement.Foreground" Value="{StaticResource ComboBoxDisabledForegroundBrush}"/>
                <Setter Property="Shape.Fill" TargetName="Arrow" Value="#999"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="{x:Type ComboBox}">
        <Setter Property="Height" Value="{DynamicResource HeightControl}" />
        <Setter Property="UIElement.SnapsToDevicePixels" Value="True"/>
        <Setter Property="FrameworkElement.OverridesDefaultStyle" Value="True"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
        <Setter Property="TextElement.Foreground" Value="Black"/>
        <Setter Property="FrameworkElement.FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="BorderThickness" Value="1.5" />
        <Setter Property="FontFamily" Value="{DynamicResource Fonts.Family}" />
        <Setter Property="MinHeight" Value="25" />
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Border Background="{TemplateBinding Background}" x:Name="Bd" BorderBrush="{DynamicResource  Brushes.Gray6}"
				 BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="2">
                        <Grid>
                            <ToggleButton Name="ToggleButton" Grid.Column="2"
                ClickMode="Press" Focusable="False"
                IsChecked="{Binding Path=IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                Template="{StaticResource ComboBoxToggleButtonTemplate}"/>

                            <ContentPresenter Name="ContentSite" Margin="5, 3, 23, 3" IsHitTestVisible="False"
                              HorizontalAlignment="Left" VerticalAlignment="Center"                              
                              Content="{TemplateBinding ComboBox.SelectionBoxItem}" 
                              ContentTemplate="{TemplateBinding ComboBox.SelectionBoxItemTemplate}"
                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"/>
                            <TextBox Name="PART_EditableTextBox" Margin="3, 3, 23, 3"                     
                     Visibility="Hidden" Background="Transparent"
                     HorizontalAlignment="Left" VerticalAlignment="Center"
                     IsReadOnly="{TemplateBinding IsReadOnly}"
                                Padding="6.5,0,6.5,0"
                     Focusable="True" >
                                <TextBox.Template>
                                    <ControlTemplate TargetType="TextBox" >
                                        <Border Name="PART_ContentHost" Focusable="False" />
                                    </ControlTemplate>
                                </TextBox.Template>
                            </TextBox>
                            <!-- Popup showing items -->
                            <Popup Name="Popup" Placement="Bottom"
                                        Focusable="False" AllowsTransparency="True"
                                        IsOpen="{TemplateBinding ComboBox.IsDropDownOpen}"
                                        PopupAnimation="Slide">
                                <Grid Name="DropDown" SnapsToDevicePixels="True"
                                        MinWidth="{TemplateBinding FrameworkElement.ActualWidth}"
                                        MaxHeight="{TemplateBinding ComboBox.MaxDropDownHeight}">
                                    <Border Name="DropDownBorder" Background="White" Margin="0, 1, 0, 0"
                                        CornerRadius="0" BorderThickness="1,1,1,1" 
                                        BorderBrush="{StaticResource ComboBoxNormalBorderBrush}"/>
                                    <ScrollViewer Margin="4" SnapsToDevicePixels="True">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="ItemsControl.HasItems" Value="False">
                            <Setter Property="FrameworkElement.MinHeight" TargetName="DropDownBorder" Value="95"/>
                        </Trigger>
                        <Trigger Property="UIElement.IsEnabled" Value="False">
                            <Setter Property="TextElement.Foreground" Value="{StaticResource ComboBoxDisabledForegroundBrush}"/>
                        </Trigger>
                        <Trigger Property="ItemsControl.IsGrouping" Value="True">
                            <Setter Property="ScrollViewer.CanContentScroll" Value="False"/>
                        </Trigger>
                        <Trigger Property="ComboBox.IsEditable" Value="True">
                            <Setter Property="KeyboardNavigation.IsTabStop" Value="False"/>
                            <Setter Property="UIElement.Visibility" TargetName="PART_EditableTextBox" Value="Visible"/>
                            <Setter Property="UIElement.Visibility" TargetName="ContentSite" Value="Hidden"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="GroupBox" x:Key="GroupBoxBase">
        <Setter Property="FontFamily" Value="{DynamicResource Fonts.Family}"/>
        <Setter Property="Padding" Value="5"/>
    </Style>
    <Style TargetType="GroupBox" BasedOn="{StaticResource GroupBoxBase}"/>
    <Style TargetType="ListView" x:Key="ListViewBase">
        <Setter Property="FontFamily" Value="{DynamicResource Fonts.Family}"/>
    </Style>
    <Style TargetType="ListView" BasedOn="{StaticResource ListViewBase}"/>
    <Style TargetType="CheckBox" x:Key="CheckBoxBase">
        <Setter Property="FontFamily" Value="{DynamicResource Fonts.Family}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CheckBox}">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                        <Border Name="border" Background="White" CornerRadius="2" BorderThickness="1.5" BorderBrush="{DynamicResource Brushes.Border}" Width="18" Height="18">
                            <Grid Name="girdBox">
                                <Grid Background="White" Margin="4" Visibility="Collapsed" Name="nullBlock" />
                                <Path Stretch="Uniform" Width="18" Height="12" Fill="White" Name="eliCheck"
                                      Data="F1 M 9.97498,1.22334L 4.6983,9.09834L 4.52164,9.09834L 0,5.19331L 1.27664,3.52165L 4.255,6.08833L 8.33331,1.52588e-005L 9.97498,1.22334 Z " Visibility="Collapsed" />
                            </Grid>
                        </Border>
                        <TextBlock Margin="5,0,0,0"  VerticalAlignment="Center" Text="{TemplateBinding Content}" />
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource Brushes.Highlight}" />
                            <Setter TargetName="border" Property="Opacity" Value="0.8" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource Brushes.AccentBase}" />
                            <Setter TargetName="border" Property="Opacity" Value="1" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="girdBox" Property="Background" Value="Gray" />
                            <Setter TargetName="border" Property="BorderBrush" Value="Gray" />
                            <Setter TargetName="girdBox" Property="Opacity" Value="0.5" />
                            <Setter TargetName="border" Property="Opacity" Value="0.5" />
                            <Setter TargetName="nullBlock" Property="Opacity" Value="0.5" />
                            <Setter TargetName="eliCheck" Property="Opacity" Value="0.5" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="eliCheck" Property="Visibility" Value="Visible"></Setter>
                            <Setter TargetName="border"  Property="BorderBrush" Value="{DynamicResource Brushes.AccentBase}" />
                            <Setter TargetName="border"  Property="Background" Value="{DynamicResource Brushes.AccentBase}" />
                            <Setter Property="Background" Value="{DynamicResource Brushes.AccentBase}" />
                            <Setter TargetName="border"  Property="BorderThickness" Value="0" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="{x:Null}">
                            <Setter TargetName="nullBlock" Property="Visibility" Value="Visible"></Setter>
                            <Setter TargetName="border"  Property="BorderBrush" Value="{DynamicResource Brushes.AccentBase}" />
                            <Setter TargetName="border"  Property="Background" Value="{DynamicResource Brushes.AccentBase}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="CheckBox" BasedOn="{StaticResource CheckBoxBase}"/>

    <Style x:Key="TextBoxBase" TargetType="TextBox">
        <Setter Property="Height" Value="{StaticResource HeightControl}" />
        <Setter Property="FontFamily" Value="{StaticResource Fonts.Family}"/>
        <Setter Property="MinWidth" Value="50" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="BorderThickness" Value="1.5" />
        <Setter Property="Padding" Value="6.5,0,6.5,0" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border Background="{TemplateBinding Background}" x:Name="Bd" BorderBrush="{DynamicResource Brushes.Border}"
		                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                        <ScrollViewer x:Name="PART_ContentHost" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" TargetName="Bd" />
                            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="TextBox" BasedOn="{StaticResource TextBoxBase}"/>

    <Style TargetType="DataGrid" x:Key="DataGridBase">
        <Setter Property="Margin" Value="0" />
        <Setter Property="FontFamily" Value="{StaticResource Fonts.Family}"/>
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="SelectionUnit" Value="FullRow" />
        <Setter Property="RowHeaderWidth" Value="0" />
        <Setter Property="SelectionMode" Value="Extended" />
        <Setter Property="AutoGenerateColumns" Value="False" />
        <Setter Property="CanUserAddRows" Value="False" />
        <Setter Property="CanUserDeleteRows" Value="False" />
        <Setter Property="CanUserResizeRows" Value="False" />
        <Setter Property="CanUserResizeColumns" Value="True" />
        <Setter Property="CanUserReorderColumns" Value="False" />
        <Setter Property="CanUserSortColumns" Value="True" />
        <Setter Property="HorizontalGridLinesBrush" Value="Gray" />
        <Setter Property="VerticalGridLinesBrush" Value="Gray" />
        <Setter Property="BorderBrush" Value="Gray" />
        <Setter Property="BorderThickness" Value="0.5" />
        <Setter Property="CanUserResizeColumns" Value="False" />
        <Setter Property="ColumnHeaderStyle">
            <Setter.Value>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="HorizontalContentAlignment" Value="Center" />
                    <Setter Property="VerticalContentAlignment" Value="Center" />
                    <Setter Property="BorderThickness" Value="0.5" />
                    <Setter Property="Height" Value="27"/>
                    <Setter Property="Background" Value="{DynamicResource Brushes.Gray5}" />
                    <Setter Property="BorderBrush" Value="Gray" />
                    <Setter Property="Foreground" Value="Black" />
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="RowStyle">
            <Setter.Value>
                <Style TargetType="DataGridRow">
                    <Setter Property="BorderThickness" Value="0"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Foreground" Value="Black"/>
                            <Setter Property="Background" Value="{DynamicResource Brushes.Gray8}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="Black"/>
                            <Setter Property="Background" Value="{DynamicResource Brushes.Gray9}"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition  Property="IsMouseOver" Value="True"/>
                                <Condition  Property="IsSelected" Value="True"/>
                            </MultiTrigger.Conditions>
                            <MultiTrigger.Setters>
                                <Setter Property="Background" Value="{DynamicResource Brushes.Gray8}"/>
                            </MultiTrigger.Setters>
                        </MultiTrigger>
                    </Style.Triggers>
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="CellStyle">
            <Setter.Value>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="4,3,4,3" />
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="HorizontalAlignment" Value="Stretch" />
                    <Setter Property="VerticalAlignment" Value="Stretch" />
                    <Setter Property="VerticalContentAlignment" Value="Center" />
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Foreground" Value="Black"/>
                            <Setter Property="Background" Value="{DynamicResource Brushes.Gray8}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="Black"/>
                            <Setter Property="Background" Value="{DynamicResource Brushes.Gray9}"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition  Property="IsMouseOver" Value="True"/>
                                <Condition  Property="IsSelected" Value="True"/>
                            </MultiTrigger.Conditions>
                            <MultiTrigger.Setters>
                                <Setter Property="Background" Value="{DynamicResource Brushes.Gray8}"/>
                            </MultiTrigger.Setters>
                        </MultiTrigger>
                    </Style.Triggers>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="DataGrid" BasedOn="{StaticResource DataGridBase}"/>
    
    <!--Customize-->
    
    
    
</ResourceDictionary>